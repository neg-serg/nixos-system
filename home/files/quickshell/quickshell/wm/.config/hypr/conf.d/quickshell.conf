# Quickshell: disable compositor animations for the app menu / overlays
#
# Applies to layer surfaces created by Quickshell with namespace "quickshell".
# If you’re on an older Hyprland that lacks layerrule, keep the windowrulev2 as well.

# Layer surfaces (preferred)
layerrule = noanim, quickshell

# Fallback for toplevels or older setups
windowrulev2 = noanim, class:^(quickshell)$

