// Minimal Vicinae extension placeholder.
// If loaded by Vicinae, it can export hooks/commands as expected by the runtime.
// Keeping it no-op to avoid runtime errors until customized.
module.exports = {
  // Example command placeholder (actual API depends on Vicinae runtime)
  // command: async () => {
  //   return { title: "Hello from neg-hello" };
  // },
};

