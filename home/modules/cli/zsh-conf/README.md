# Zsh Config

This directory is symlinked to `~/.config/zsh` via Home Manager using an out-of-store symlink, so
edits take effect immediately without rebuilding.

Typical layout:

- `.zshrc`
- `rc.d/` (sourced extras)
- `plugins/` (custom plugins if not installed via Nix)
