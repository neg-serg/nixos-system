# ===== Embeddings =====
EMBEDDINGS_PROVIDER=openai
OPENAI_API_KEY=sk-your-key
OPENAI_BASE_URL=
OPENAI_EMBED_MODEL=text-embedding-3-small
OPENAI_EMBED_DIM=1536
# TEI alternative:
# TEI_ENDPOINT=http://localhost:8080/embeddings

# ===== Image-to-Text =====
ENABLE_IMAGE_TO_TEXT=false
IMAGE_TO_TEXT_PROVIDER=openai
IMAGE_TO_TEXT_MODEL=gpt-4o-mini

# ===== Confluence =====
CONFLUENCE_BASE_URL=
CONFLUENCE_EMAIL=
CONFLUENCE_API_TOKEN=
# Authentication method: 'basic' for Cloud (email:token), 'bearer' for Server (personal access token)
CONFLUENCE_AUTH_METHOD=basic
CONFLUENCE_SPACES=
# Optional: Filter by parent page IDs (comma-separated Confluence page IDs)
# Only pages under these parent pages (and their children) will be indexed
CONFLUENCE_PARENT_PAGES=
# Optional: Title patterns to include (comma-separated, supports * wildcards)
# Examples: "API*", "User Guide", "*Documentation*"
CONFLUENCE_TITLE_INCLUDES=
# Optional: Title patterns to exclude (comma-separated, supports * wildcards)
# Examples: "Draft*", "*Archive*", "Test*"
CONFLUENCE_TITLE_EXCLUDES=

# ===== Local Files =====
FILE_ROOTS=.
FILE_INCLUDE_GLOBS=**/*.{go,ts,tsx,js,py,rs,java,md,mdx,txt,yaml,yml,json,pdf,png,jpg,jpeg,gif,svg,webp}
FILE_EXCLUDE_GLOBS=**/{.git,node_modules,dist,build,target}/**

# ===== Database =====
# Database type: 'sqlite' (default) or 'postgresql'
DB_TYPE=sqlite

# SQLite database path (used when DB_TYPE=sqlite)
DB_PATH=./data/index.db

# PostgreSQL connection string (used when DB_TYPE=postgresql)
# POSTGRES_CONNECTION_STRING=postgresql://username:password@localhost:5432/docsearch
