diff --git a/flake.nix b/flake.nix
--- a/flake.nix
+++ b/flake.nix
@@
     # CamelCase alias for convenience in code
     homeManagerInput.follows = "home-manager";
-    # Pin hy3 to tag compatible with Hyprland v0.52.x
-    hy3 = {
-      # hy3 tags track Hyprland compatibility (hlX.Y.Z)
-      url = "github:outfoxxed/hy3?ref=hl0.52.0"; # hl0.52.0
-      # Ensure hy3 uses the same Hyprland input we pin below
-      inputs.hyprland.follows = "hyprland";
-    };
     # Pin Hyprland to v0.52.0 (stable)
     hyprland = {
       url = "github:hyprwm/Hyprland?ref=v0.52.0";
       inputs.nixpkgs.follows = "nixpkgs";
     };
@@
   outputs = inputs @ {
     self,
     chaotic,
     homeManagerInput,
-    hy3,
     iosevkaNegInput,
     nixpkgs,
     nur,
     sopsNixInput,
     stylixInput,
@@
   # mkHMArgs moved to a helper; keep semantics identical
   mkHMArgs = import ./flake/mkHMArgs.nix {
-    inherit lib perSystem hy3 yandexBrowserInput nur inputs;
+    inherit lib perSystem yandexBrowserInput nur inputs;
     inherit hmInputs extraSubstituters extraTrustedKeys;
   };
@@
         packages = let
           extrasFlag = boolEnv "HM_EXTRAS";
           extrasSet = import ./flake/pkgs-extras.nix {
-            inherit hy3 pkgs system;
+            inherit pkgs system;
           };
         in
           {
             default = pkgs.zsh;
           }
diff --git a/flake/mkHMArgs.nix b/flake/mkHMArgs.nix
--- a/flake/mkHMArgs.nix
+++ b/flake/mkHMArgs.nix
@@
 {
   lib,
   perSystem,
-  hy3,
   yandexBrowserInput,
   nur,
   inputs,
   hmInputs,
   extraSubstituters,
   extraTrustedKeys,
 }: system: {
   inputs = hmInputs;
-  inherit hy3;
+  # hy3 больше не тянем как flake‑инпут (используем системный плагин)
+  hy3 = {};
   inherit (perSystem.${system}) iosevkaNeg;
@@
   yandexBrowserProvider = pkgs:
     yandexBrowserInput.packages.${pkgs.stdenv.hostPlatform.system};
 }
diff --git a/flake/pkgs-extras.nix b/flake/pkgs-extras.nix
--- a/flake/pkgs-extras.nix
+++ b/flake/pkgs-extras.nix
@@
-{
-  hy3,
-  system,
-  pkgs,
-}: {
-  hy3Plugin = hy3.packages.${system}.hy3;
+{
+  system,
+  pkgs,
+}: {
+  hy3Plugin = pkgs.hyprlandPlugins.hy3;
   bpf-host-latency = pkgs.neg.bpf_host_latency;
 }
diff --git a/modules/user/gui/hyprland/core.nix b/modules/user/gui/hyprland/core.nix
--- a/modules/user/gui/hyprland/core.nix
+++ b/modules/user/gui/hyprland/core.nix
@@
 {
   lib,
   config,
   pkgs,
   xdg,
-  hy3, # flake input (passed via mkHMArgs) to locate hy3 plugin path
   inputs, # expose flake inputs (hyprland) to align runtime with hy3
   raiseProvider ? null,
   ...
 }:
@@
     # Dynamically generated plugin loader (hy3 only)
     (mkIf (config.features.gui.hy3.enable or false) (
-      let
-        # Resolve hy3 plugin for the current system; keep out of closure churn elsewhere
-        hy3Pkg = hy3.packages.${pkgs.stdenv.hostPlatform.system}.hy3;
-        pluginPath = "${hy3Pkg}/lib/libhy3.so";
-      in
       xdg.mkXdgText "hypr/plugins.conf" ''
         # Hyprland plugins
-        plugin = ${pluginPath}
+        plugin = /etc/static/hypr/libhy3.so
       ''
     ))
     # Overwrite existing generated plugin config file if present
     (mkIf (config.features.gui.hy3.enable or false) { xdg.configFile."hypr/plugins.conf".force = true; })
-    # Keep the hy3 plugin alive in the profile to avoid GC removing the path
-    (mkIf (config.features.gui.hy3.enable or false) { home.packages = [ (hy3.packages.${pkgs.stdenv.hostPlatform.system}.hy3) ]; })
   ])
diff --git a/modules/features.nix b/modules/features.nix
--- a/modules/features.nix
+++ b/modules/features.nix
@@
 {
   lib,
   config,
   inputs,
-  hy3,
   ...
 }:
@@
-      assertions =
-        # Hypr/hy3 compatibility check only matters when GUI and hy3 are enabled
-        (lib.optionals (cfg.gui.enable && cfg.gui.hy3.enable) [
-          (let
-            # Avoid evaluating inputs.hyprland.packages; read version from pinned ref if available.
-            hyprlandRef = lib.attrByPath ["original" "ref"] null inputs.hyprland;
-            hyprlandVersion = let
-              # Fallback: strip leading 'v' from ref like "v0.50.1"
-              refNorm =
-                if hyprlandRef == null
-                then null
-                else
-                  (let
-                    s = toString hyprlandRef;
-                  in
-                    if lib.hasPrefix "v" s
-                    then builtins.substring 1 (builtins.stringLength s - 1) s
-                    else s);
-            in
-              refNorm;
-            hy3Rev = lib.attrByPath ["rev"] null hy3;
-            compatible = [
-              { hv = "0.50.1"; rev = "1fdc0a291f8c23b22d27d6dabb466d018757243c"; }
-              { hv = "0.51.0"; rev = "e317a4cf89486f33c0e09364fbb6949e9f4f5624"; }
-              { hv = "0.51.1"; rev = "e317a4cf89486f33c0e09364fbb6949e9f4f5624"; }
-            ];
-            matches = c: (hyprlandVersion == null || hyprlandVersion == c.hv) && (hy3Rev == null || hy3Rev == c.rev);
-            ok = lib.any matches compatible;
-          in {
-            assertion = ok;
-            message = ''
-              Incompatible Hyprland/hy3 pins detected.
-              Expected one of: ${builtins.concatStringsSep ", " (map (c: "Hyprland " + c.hv + " + hy3 " + builtins.substring 0 7 c.rev) compatible)}
-              Got: Hyprland ${toString (
-                if hyprlandVersion == null
-                then "<unknown>"
-                else hyprlandVersion
-              )} + hy3 ${toString (
-                if hy3Rev == null
-                then "<unknown>"
-                else builtins.substring 0 7 hy3Rev
-              )}
-              Update flake.nix pins or extend the compatibility matrix in modules/features.nix.
-            '';
-          })
-        ])
-        ++ [
+      assertions =
+        [
           {
             assertion = cfg.gui.enable || (! cfg.gui.qt.enable);
             message = "features.gui.qt.enable requires features.gui.enable = true";
           }
           {
