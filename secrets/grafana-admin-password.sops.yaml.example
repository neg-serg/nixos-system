# Stub file for Grafana admin password managed by SOPS.
#
# Usage:
#   1) Copy to secrets/grafana-admin-password.sops.yaml
#   2) Put a single-line strong password below (no quotes)
#   3) Encrypt in-place: sops -e -i secrets/grafana-admin-password.sops.yaml
#   4) Commit the encrypted file and rebuild/switch
#
# This file is NOT used by the system; only the encrypted
# secrets/grafana-admin-password.sops.yaml or secrets/grafana-admin-password.sops is.

CHANGE_ME_STRONG_PASSWORD

