# Documentation

The canonical English manual now lives in `docs/manual/manual.en.md`. It now covers both the system
modules and the legacy Home Manager tree under `home/`, so you only need to read that file for
instructions and agent tips.
